/target

# Secrets — never committed (see ADR-005; snapshot files are RAM dumps)
.env
*.env

# Local config (the committed sample is sleepwalk.example.toml)
/sleepwalk.toml

# Prometheus scrape targets — may hold real host addresses (committed sample is
# targets.json.example). Keep your hosts out of version control.
/deploy/prometheus/targets.json

# Fetched artifacts (pinned + hashed in images/versions.toml, fetched by `just fetch`)
/images/artifacts/

# Snapshot dirs are RAM dumps — secret-bearing, 0700, never committed
*.snap/
/run/

# Scratch results (only curated samples are committed under results/)
/results/scratch/

# Local dev-VM workspace: disk images, cloud-init seeds, the generated ssh
# PRIVATE KEY, UEFI vars, boot logs, throwaway smoke scripts. Never committed.
/images/dev-vm/
*.qcow2
*.img
seed.iso
