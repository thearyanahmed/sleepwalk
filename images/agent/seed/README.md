# calc task

Seed working tree for the agent demo. The coding agent (aider) builds out
`calc.py` and `test_calc.py` over several turns; a migration lands in an idle gap
between turns and the agent continues, unaware it moved hosts.
